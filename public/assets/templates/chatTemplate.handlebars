<h2 id="chat__title">Chat</h2>
<div id="chat_dashboard">
{{#if messages.length}}
    {{#each messages}}
    <div class="chat-message">
        <p class="chat-message-text">{{this.text}}</p>
        <p class="chat-message-date">{{this.author.id}}</p>
    </div>
    {{/each}}
{{/if}}
</div>